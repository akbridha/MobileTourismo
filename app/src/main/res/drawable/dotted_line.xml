<?xml version="1.0" encoding="utf-8"?>

    <shape xmlns:android="http://schemas.android.com/apk/res/android">
        <stroke
            android:width="2dp"
        android:color="@color/teal_700" /> <!-- Warna garis, sesuaikan dengan kebutuhan -->
        <size android:height="2dp" /> <!-- Tinggi garis, sesuaikan dengan kebutuhan -->
        <solid android:color="@android:color/transparent" /> <!-- Warna background transparan -->
        <corners android:radius="8dp" /> <!-- Tidak ada sudut melengkung -->
        <padding android:left="0dp" android:top="0dp" android:right="0dp"
            android:bottom="0dp" /> <!-- Padding nol -->
        <stroke
            android:color="@color/teal_700"
        android:width="2dp"
        android:dashWidth="8dp"
        android:dashGap="4dp" />

</shape>